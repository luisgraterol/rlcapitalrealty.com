---
---

<div class="page">
  <div class="portal-content">

    <div class="section-label">Property Financial Analyzer</div>

    <div class="calc-layout">

      <!-- INPUT PANEL -->
      <div class="calc-panel">
        <div class="calc-panel-title">
          Inputs
          <span id="current-analysis-id" style="display:none"></span>
        </div>

        <!-- Property Association -->
        <div class="property-assoc">
          <div class="property-assoc-row">
            <div class="field-group" style="flex:1;min-width:0">
              <label for="inp-property">Linked Property</label>
              <select id="inp-property">
                <option value="">— Select a property —</option>
              </select>
            </div>
            <button id="btn-new-property" type="button" class="btn-add-prop" title="Add new property">
              <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
              New
            </button>
          </div>
          <div id="orphan-warning" class="assoc-banner warn" style="display:none">
            This analysis has no linked property. Select one above before saving.
          </div>
          <div id="no-property-hint" class="assoc-banner info" style="display:none">
            Select a property above to enable saving.
          </div>
          <div id="no-properties-hint" class="assoc-banner info" style="display:none">
            No properties yet. <a href="/admin/properties">Create one first →</a>
          </div>
        </div>

        <!-- Section 1: Property Basics -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-basics" type="button">
            Property Basics
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-basics">
            <div class="field-row full">
              <div class="field-group">
                <label for="inp-name">Analysis Label</label>
                <input type="text" id="inp-name" value="New Analysis" placeholder="e.g. 1234 Oak St — Summer" />
              </div>
            </div>
            <div class="field-row full">
              <div class="field-group">
                <label for="inp-address">Property Address</label>
                <input type="text" id="inp-address" placeholder="Auto-filled from linked property" readonly />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-bedrooms">Bedrooms</label>
                <input type="text" id="inp-bedrooms" disabled class="prop-auto-field" placeholder="— from property —" />
              </div>
              <div class="field-group">
                <label for="inp-bathrooms">Bathrooms</label>
                <input type="text" id="inp-bathrooms" disabled class="prop-auto-field" placeholder="— from property —" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-type">Property Type</label>
                <input type="text" id="inp-type" disabled class="prop-auto-field" placeholder="— from property —" />
              </div>
              <div class="field-group"></div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-rent-ask">Monthly Rent (Ask)</label>
                <input type="number" id="inp-rent-ask" value="1400" min="0" step="50" />
              </div>
              <div class="field-group">
                <label for="inp-rent-neg">Monthly Rent (Negotiated)</label>
                <input type="number" id="inp-rent-neg" value="1200" min="0" step="50" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-deposit">Security Deposit</label>
                <input type="number" id="inp-deposit" value="1400" min="0" step="50" />
              </div>
              <div class="field-group">
                <label for="inp-hoa">HOA Present?</label>
                <select id="inp-hoa">
                  <option value="No" selected>No</option>
                  <option value="Yes">Yes</option>
                </select>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-walk-away">Walk-Away Threshold ($)</label>
                <input type="number" id="inp-walk-away" value="1750" min="0" step="50" />
                <span class="field-note">Max monthly rent R&amp;L will sign</span>
              </div>
              <div class="field-group"></div>
            </div>
            <div class="hoa-block" id="hoa-block">
              <div class="hoa-block-title">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
                HOA Detected — Property Disqualified
              </div>
              <p class="hoa-block-body">R&amp;L Capital Realty does not lease HOA properties under any circumstances.</p>
            </div>
          </div>
        </div>

        <!-- Section 2: Revenue Assumptions -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-revenue" type="button">
            Revenue Assumptions
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-revenue">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-adr">ADR — Avg Daily Rate ($)</label>
                <input type="number" id="inp-adr" value="167" min="0" step="1" />
                <span class="field-note">AirDNA Abilene 2BR avg</span>
              </div>
              <div class="field-group">
                <label for="inp-occ">Occupancy Rate (%)</label>
                <input type="number" id="inp-occ" value="77" min="0" max="100" step="1" />
                <span class="field-note">AirDNA Abilene annual avg</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-stay">Avg Stay Length (nights)</label>
                <input type="number" id="inp-stay" value="3.2" min="1" step="0.1" />
                <span class="field-note">Abilene military/university profile</span>
              </div>
              <div class="field-group">
                <label for="inp-demand">Primary Demand Driver</label>
                <select id="inp-demand">
                  <option value="Military TDY" selected>Military TDY</option>
                  <option value="University Events">University Events</option>
                  <option value="Mixed">Mixed</option>
                  <option value="Corporate/Contractor">Corporate / Contractor</option>
                </select>
              </div>
            </div>
          </div>
        </div>

        <!-- Section 3: Fixed Monthly Costs -->
        <div class="calc-section">
          <button class="calc-section-toggle open" data-target="sec-fixed" type="button">
            Fixed Monthly Costs
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body open" id="sec-fixed">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-utilities">Utilities (elec/water/gas)</label>
                <input type="number" id="inp-utilities" value="185" min="0" step="5" />
              </div>
              <div class="field-group">
                <label for="inp-internet">Internet (fiber)</label>
                <input type="number" id="inp-internet" value="80" min="0" step="1" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-insurance">STR Insurance (Proper)</label>
                <input type="number" id="inp-insurance" value="100" min="0" step="1" />
                <span class="field-note">Quote in hand: $98–103/mo</span>
              </div>
              <div class="field-group">
                <label for="inp-supplies">Supplies &amp; Amenities</label>
                <input type="number" id="inp-supplies" value="120" min="0" step="5" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-pms">PMS Software (Hospitable)</label>
                <input type="number" id="inp-pms" value="39" min="0" step="1" />
              </div>
              <div class="field-group">
                <label for="inp-pricing">Dynamic Pricing (PriceLabs)</label>
                <input type="number" id="inp-pricing" value="0" min="0" step="1" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label>Maintenance Reserve</label>
                <input type="number" id="inp-maintenance" readonly />
                <span class="field-note">Auto: 1.5% of negotiated rent (min $75, max $150)</span>
              </div>
              <div class="field-group">
                <label>STR Permit</label>
                <input type="number" value="0" readonly disabled />
                <span class="field-note"><span class="field-badge">No permit required — City of Abilene (2026)</span></span>
              </div>
            </div>
          </div>
        </div>

        <!-- Section 4: Per-Stay Variable Costs -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-variable" type="button">
            Per-Stay Variable Costs
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-variable">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-cleaning">Cleaning Fee per Stay ($)</label>
                <input type="number" id="inp-cleaning" value="120" min="0" step="5" />
                <span class="field-note">Pass-through: charged to guest = paid to cleaner. Net P&amp;L impact = $0.</span>
              </div>
              <div class="field-group">
                <label>Airbnb Host Fee</label>
                <input type="number" value="3" readonly disabled />
                <span class="field-note">Standard 3% — not editable</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Section 5: Setup & One-Time Costs -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-setup" type="button">
            Setup &amp; One-Time Costs
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-setup">
            <div class="field-row">
              <div class="field-group">
                <label for="inp-furniture">Furniture &amp; Décor</label>
                <input type="number" id="inp-furniture" value="6500" min="0" step="100" />
                <span class="field-note">2BR standard</span>
              </div>
              <div class="field-group">
                <label for="inp-photo">Photography</label>
                <input type="number" id="inp-photo" value="200" min="0" step="50" />
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-lock">Smart Lock</label>
                <input type="number" id="inp-lock" value="150" min="0" step="10" />
              </div>
              <div class="field-group">
                <label>LLC Formation</label>
                <input type="number" value="0" readonly disabled />
                <span class="field-note">Already registered — locked at $0</span>
              </div>
            </div>
            <div class="field-row">
              <div class="field-group">
                <label for="inp-legal">Legal (Sublease Addendum)</label>
                <input type="number" id="inp-legal" value="300" min="0" step="50" />
                <span class="field-note">Texas RE attorney review</span>
              </div>
              <div class="field-group">
                <label for="inp-misc">Miscellaneous Buffer</label>
                <input type="number" id="inp-misc" value="500" min="0" step="50" />
              </div>
            </div>
          </div>
        </div>

        <!-- Section 6: HOT Tax Info -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-hot" type="button">
            HOT Tax (Abilene)
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-hot">
            <div class="hot-info">
              <p><strong>Hotel Occupancy Tax (HOT) — Abilene, TX</strong><br />
              HOT tax is collected and remitted directly by Airbnb on behalf of the host.
              It does not appear as a cost to the operator and does not impact the P&amp;L model.
              No action required.</p>
            </div>
          </div>
        </div>

        <!-- Notes -->
        <div class="calc-section">
          <button class="calc-section-toggle" data-target="sec-notes" type="button">
            Notes
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
          <div class="calc-section-body" id="sec-notes">
            <div class="field-row full">
              <div class="field-group">
                <label for="inp-notes">Notes</label>
                <textarea id="inp-notes" placeholder="Landlord contact, property condition, negotiation notes…"></textarea>
              </div>
            </div>
          </div>
        </div>

      </div><!-- /calc-panel -->

      <!-- OUTPUT PANEL -->
      <div class="output-panel" id="output-panel">

        <div class="hoa-block" id="hoa-save-block">
          <div class="hoa-block-title">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
            HOA Detected — Property Disqualified
          </div>
          <p class="hoa-block-body">Saving and export are disabled. R&amp;L Capital Realty does not lease HOA properties.</p>
        </div>

        <!-- Section 1: Property header -->
        <div class="res-header">
          <div class="res-header-main">
            <div class="res-address" id="res-address">—</div>
            <div class="res-subline" id="res-subline">Select a property to begin</div>
          </div>
          <div class="res-header-side">
            <div class="res-buffer-label">Break-even buffer</div>
            <div class="res-buffer-val" id="res-buffer">—</div>
          </div>
        </div>
        <div id="res-badge" class="res-badge" style="display:none"></div>

        <!-- Section 2: Metric cards -->
        <div class="res-metrics">
          <div class="res-metric" id="rmc-net-monthly">
            <div class="rmc-val" id="res-net-monthly">—</div>
            <div class="rmc-label">Net / month</div>
            <div class="rmc-sub" id="res-margin-sub">— margin</div>
          </div>
          <div class="res-metric" id="rmc-net-annual">
            <div class="rmc-val" id="res-net-annual">—</div>
            <div class="rmc-label">Net / year</div>
            <div class="rmc-sub">base case</div>
          </div>
          <div class="res-metric">
            <div class="rmc-val" id="res-roi12">—</div>
            <div class="rmc-label">12-mo ROI</div>
            <div class="rmc-sub" id="res-roi-sub">—</div>
          </div>
          <div class="res-metric">
            <div class="rmc-val" id="res-payback">—</div>
            <div class="rmc-label">Payback</div>
            <div class="rmc-sub" id="res-mult-sub">—</div>
          </div>
        </div>

        <!-- Section 3: Two-column chart row -->
        <div class="res-chart-row">
          <div class="res-chart-card">
            <div class="rcc-title">Revenue waterfall</div>
            <div style="position:relative;height:220px">
              <canvas id="chart-waterfall"></canvas>
            </div>
          </div>
          <div class="res-chart-card">
            <div class="rcc-title">Break-even vs actual occupancy</div>
            <div class="gauge-wrap">
              <canvas id="chart-gauge" width="220" height="130"></canvas>
            </div>
            <div class="gauge-legend" id="gauge-legend"></div>
            <div class="rcc-title" style="margin-top:14px">Scenario outcomes</div>
            <div style="position:relative;height:80px">
              <canvas id="chart-scenarios"></canvas>
            </div>
          </div>
        </div>

        <!-- Section 4: Monthly cash flow -->
        <div class="res-chart-card">
          <div class="rcc-title">Estimated monthly cash flow</div>
          <div style="position:relative;height:140px">
            <canvas id="chart-monthly"></canvas>
          </div>
          <div class="rcc-footnote" id="monthly-footnote"></div>
          <div class="rcc-market-note" id="market-note"></div>
        </div>

        <!-- Section 5: ROI curve -->
        <div class="res-chart-card">
          <div class="rcc-title">Cumulative ROI — 24-month payback curve</div>
          <div style="position:relative;height:140px">
            <canvas id="chart-roi"></canvas>
          </div>
          <div class="rcc-footnote" id="roi-footnote"></div>
        </div>

        <!-- Risk flags (collapsible) -->
        <details class="res-collapsible">
          <summary class="res-coll-hd">
            <span>Risk Assessment</span>
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </summary>
          <div class="res-coll-body">
            <div class="risk-flags" id="risk-flags">
              <div class="risk-flag ok"><span class="risk-flag-icon">✓</span>No risk flags triggered.</div>
            </div>
          </div>
        </details>

        <!-- Cleaning pass-through (collapsible) -->
        <details class="res-collapsible">
          <summary class="res-coll-hd">
            <span>Cleaning — Pass-Through</span>
            <svg viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"/></svg>
          </summary>
          <div class="res-coll-body">
            <div class="passthrough-note">Revenue collected from guest = cost paid to cleaner. Net P&amp;L impact = $0.</div>
            <div class="metric-row"><span class="metric-label">Stays / Month</span><span class="metric-value" id="out-stays">—</span></div>
            <div class="metric-row"><span class="metric-label">Cleaning Collected</span><span class="metric-value" id="out-cleaning-collected">—</span></div>
            <div class="metric-row"><span class="metric-label">Cleaning Paid</span><span class="metric-value" id="out-cleaning-paid">—</span></div>
            <div class="metric-row"><span class="metric-label">Net P&amp;L Impact</span><span class="metric-value green">$0</span></div>
          </div>
        </details>

        <!-- Actions -->
        <div class="calc-actions">
          <button class="btn btn-primary" id="btn-save" type="button" disabled>
            <svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
            Save Analysis
          </button>
          <button class="btn btn-ghost" id="btn-update" type="button" style="display:none">
            <svg viewBox="0 0 24 24"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg>
            Update
          </button>
          <button class="btn btn-ghost" id="btn-new" type="button" style="display:none">New Analysis</button>
          <button class="btn btn-ghost" id="btn-export" type="button">
            <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
            Export .md
          </button>
        </div>

        <div class="view-saved-link">
          <a href="/admin/analyses">View all saved analyses →</a>
        </div>

      </div><!-- /output-panel -->
    </div><!-- /calc-layout -->

  </div><!-- /portal-content -->
</div><!-- /page -->

<footer>
  <div class="footer-brand">R<span>&amp;</span>L Capital Realty LLC</div>
  <div class="footer-meta">&copy; 2026 &middot; Abilene, TX &middot; Owner Portal</div>
</footer>

<!-- New Property Modal -->
<div class="modal-backdrop" id="modal-backdrop" style="display:none"></div>
<div class="modal" id="modal-new-property" style="display:none" role="dialog" aria-modal="true">
  <div class="modal-header">
    <div class="modal-title">Add Property</div>
    <button class="modal-close" id="modal-close" type="button" aria-label="Close">&times;</button>
  </div>
  <div class="modal-body">
    <div class="modal-error" id="modal-error" style="display:none"></div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-addr1">Address Line 1 <span class="req">*</span></label>
        <input type="text" id="np-addr1" placeholder="123 Main St" />
      </div>
    </div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-addr2">Address Line 2</label>
        <input type="text" id="np-addr2" placeholder="Apt 2B, Unit 4…" />
      </div>
    </div>
    <div class="field-row">
      <div class="field-group">
        <label for="np-city">City <span class="req">*</span></label>
        <input type="text" id="np-city" placeholder="Abilene" />
      </div>
      <div class="field-group">
        <label for="np-zip">Zip Code <span class="req">*</span></label>
        <input type="text" id="np-zip" placeholder="79601" />
      </div>
    </div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-neighborhood">Neighborhood</label>
        <input type="text" id="np-neighborhood" placeholder="Optional" />
      </div>
    </div>
    <div class="field-row">
      <div class="field-group">
        <label for="np-bedrooms">Bedrooms</label>
        <select id="np-bedrooms">
          <option value="">—</option>
          <option value="1">1 BR</option>
          <option value="2">2 BR</option>
          <option value="3">3 BR</option>
          <option value="4">4 BR</option>
          <option value="5">5+ BR</option>
        </select>
      </div>
      <div class="field-group">
        <label for="np-bathrooms">Bathrooms</label>
        <select id="np-bathrooms">
          <option value="">—</option>
          <option value="1">1 BA</option>
          <option value="1.5">1.5 BA</option>
          <option value="2">2 BA</option>
          <option value="2.5">2.5 BA</option>
          <option value="3">3 BA</option>
          <option value="3.5">3.5 BA</option>
          <option value="4">4 BA</option>
        </select>
      </div>
    </div>
    <div class="field-row">
      <div class="field-group">
        <label for="np-type">Property Type</label>
        <select id="np-type">
          <option value="">—</option>
          <option value="House">House</option>
          <option value="Apartment">Apartment</option>
          <option value="Condo">Condo</option>
          <option value="Townhouse">Townhouse</option>
          <option value="Duplex">Duplex</option>
        </select>
      </div>
      <div class="field-group"></div>
    </div>
    <div class="field-row full">
      <div class="field-group">
        <label for="np-landlord">Landlord <span class="req">*</span></label>
        <select id="np-landlord">
          <option value="">— Select landlord —</option>
        </select>
      </div>
    </div>
    <div id="no-landlords-msg" class="assoc-banner warn" style="display:none">
      No landlords yet. <a href="/admin/landlords">Create one first →</a>
    </div>

    <!-- Quick-create landlord toggle -->
    <div class="quick-landlord-toggle">
      <button type="button" id="btn-toggle-ql" class="link-btn">
        <svg viewBox="0 0 24 24"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        Quick-create landlord
      </button>
    </div>
    <div id="quick-landlord-form" class="quick-landlord-form" style="display:none">
      <div class="ql-title">New Landlord</div>
      <div class="field-row full">
        <div class="field-group">
          <label for="ql-name">Name <span class="req">*</span></label>
          <input type="text" id="ql-name" placeholder="Acme Property Mgmt" />
        </div>
      </div>
      <div class="field-row">
        <div class="field-group">
          <label for="ql-phone">Phone <span class="req">*</span></label>
          <input type="text" id="ql-phone" placeholder="(555) 000-0000" />
        </div>
        <div class="field-group">
          <label for="ql-email">Email <span class="req">*</span></label>
          <input type="email" id="ql-email" placeholder="contact@example.com" />
        </div>
      </div>
      <div class="field-row full">
        <div class="field-group">
          <label for="ql-type">Type <span class="req">*</span></label>
          <select id="ql-type">
            <option value="">— Select type —</option>
            <option>Property Management Company</option>
            <option>Individual Investor's LLC</option>
            <option>Small Local RE Company</option>
            <option>Regional Portfolio Operator</option>
            <option>Institutional Landlord</option>
            <option>Real Estate Agent</option>
            <option>Turnkey Rental Company</option>
            <option>Relocation/Corporate Housing Company</option>
          </select>
        </div>
      </div>
      <button type="button" class="btn btn-ghost btn-sm" id="btn-create-landlord">Create Landlord</button>
    </div>

    <div class="field-row full" style="margin-top:12px">
      <div class="field-group">
        <label for="np-notes">Notes</label>
        <textarea id="np-notes" placeholder="Optional"></textarea>
      </div>
    </div>

    <div class="modal-actions">
      <button type="button" class="btn btn-ghost" id="btn-modal-cancel">Cancel</button>
      <button type="button" class="btn btn-primary" id="btn-modal-save">Create Property</button>
    </div>
  </div>
</div>

<script>
  import { Chart } from 'chart.js/auto';
  import { fmt, fmtPct, fmtX, colorClass } from '@lib/formatters';
  import { calcAll, calcMaintenance, getRiskFlags } from '@lib/calculations';
  import type { AnalysisInputs } from '@lib/calculations';
  import { supabase } from '@lib/supabase';
  import { requireAuth } from '@lib/auth';

  // ── Color palette ─────────────────────────────────────────────────────────
  const C = { green: '#1D9E75', blue: '#378ADD', red: '#E24B4A' };
  const isDark = () => window.matchMedia('(prefers-color-scheme: dark)').matches;
  const gridColor = () => isDark() ? 'rgba(255,255,255,0.06)' : 'rgba(0,0,0,0.06)';
  const tickColor = () => isDark() ? '#a0a09a' : '#5f5e5a';
  const fmtK = (v: number) => {
    const a = Math.abs(v);
    return (v < 0 ? '-' : '') + '$' + (a >= 1000 ? (a / 1000).toFixed(1) + 'k' : Math.round(a).toLocaleString());
  };

  // ── Chart instances ───────────────────────────────────────────────────────
  let chartWaterfall: Chart | null = null;
  let chartScenarios: Chart | null = null;
  let chartMonthly:   Chart | null = null;
  let chartRoi:       Chart | null = null;

  // ── DOM helpers ──────────────────────────────────────────────────────────

  function setOut(id: string, text: string, cls?: string) {
    const el = document.getElementById(id);
    if (!el) return;
    el.textContent = text;
    el.className = 'metric-value' + (cls ? ' ' + cls : '');
  }

  function inp(id: string): number {
    const el = document.getElementById(id) as HTMLInputElement | null;
    return el ? parseFloat(el.value) || 0 : 0;
  }

  function inpStr(id: string): string {
    const el = document.getElementById(id) as HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement | null;
    return el ? el.value.trim() : '';
  }

  function esc(str: string): string {
    return String(str)
      .replace(/&/g, '&amp;').replace(/</g, '&lt;')
      .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
  }

  function show(id: string) { const el = document.getElementById(id); if (el) el.style.display = ''; }
  function hide(id: string) { const el = document.getElementById(id); if (el) el.style.display = 'none'; }

  // ── State ─────────────────────────────────────────────────────────────────

  let currentAnalysisId: string | null = null;
  let selectedPropertyId: string | null = null;
  let propertiesList: any[] = [];
  let lastResults: ReturnType<typeof calcAll> | null = null;

  // ── Accordion ────────────────────────────────────────────────────────────

  document.querySelectorAll<HTMLButtonElement>('.calc-section-toggle').forEach(btn => {
    btn.addEventListener('click', () => {
      const id   = btn.dataset.target!;
      const body = document.getElementById(id)!;
      const open = body.classList.contains('open');
      body.classList.toggle('open', !open);
      btn.classList.toggle('open', !open);
    });
  });

  // ── Monthly occupancy profiles ────────────────────────────────────────────
  function getMonthlyProfile(driver: string, occPct: number) {
    const b = occPct / 100;
    const clamp = (v: number) => Math.min(1, Math.max(0, v));
    const profiles: Record<string, { marketType: string; note: string; factors: number[] }> = {
      'Military TDY': {
        marketType: 'year-round',
        note: 'Seasonal factors estimated from typical military TDY patterns — near-flat year-round. Verify against your own PMS data before relying on monthly projections.',
        factors: [0.95, 0.96, 0.98, 1.01, 1.02, 1.03, 1.04, 1.03, 1.02, 1.01, 0.98, 0.97],
      },
      'University Events': {
        marketType: 'vacational',
        note: 'Seasonal factors estimated from typical university demand curves — peaks in fall/spring semesters, significant summer drop. Verify against AirDNA seasonality data.',
        factors: [1.10, 1.08, 0.95, 0.88, 0.80, 0.70, 0.68, 0.75, 1.12, 1.15, 1.10, 0.90],
      },
      'Corporate/Contractor': {
        marketType: 'year-round',
        note: 'Seasonal factors estimated from typical corporate travel patterns — consistent with minor summer slowdown. Verify against your own PMS data.',
        factors: [1.02, 1.02, 1.03, 1.02, 1.01, 0.96, 0.94, 0.95, 1.01, 1.02, 1.02, 0.98],
      },
      'Mixed': {
        marketType: 'mixed',
        note: 'Seasonal factors estimated from a blended demand profile — moderate variance year-round. Verify against AirDNA seasonality data for this market.',
        factors: [0.97, 0.96, 0.98, 1.00, 1.01, 1.02, 1.03, 1.02, 1.01, 1.02, 1.00, 0.98],
      },
    };
    const p = profiles[driver] ?? profiles['Mixed'];
    return { marketType: p.marketType, note: p.note, occs: p.factors.map(f => clamp(b * f)) };
  }

  // ── Gauge (canvas 2D semicircle) ──────────────────────────────────────────
  function drawGauge(breakEvenPct: number, actualPct: number) {
    const canvas = document.getElementById('chart-gauge') as HTMLCanvasElement | null;
    if (!canvas) return;
    const ctx = canvas.getContext('2d')!;
    const W = canvas.width, H = canvas.height;
    ctx.clearRect(0, 0, W, H);
    const cx = W / 2, cy = H - 8;
    const r  = Math.min(cx - 12, cy - 6);
    const lw = 13;
    // angle: 0% → π (left/9 o'clock), 100% → 2π (right/3 o'clock), clockwise through top
    const toAngle = (pct: number) => Math.PI + (Math.min(Math.max(pct, 0), 100) / 100) * Math.PI;
    // background
    ctx.beginPath(); ctx.arc(cx, cy, r, Math.PI, 2 * Math.PI, false);
    ctx.strokeStyle = isDark() ? '#333' : '#e0ddd8'; ctx.lineWidth = lw; ctx.lineCap = 'butt'; ctx.stroke();
    // red: 0 → breakEven
    ctx.beginPath(); ctx.arc(cx, cy, r, Math.PI, toAngle(breakEvenPct), false);
    ctx.strokeStyle = C.red; ctx.lineWidth = lw; ctx.stroke();
    // green: breakEven → actual
    if (actualPct > breakEvenPct) {
      ctx.beginPath(); ctx.arc(cx, cy, r, toAngle(breakEvenPct), toAngle(actualPct), false);
      ctx.strokeStyle = C.green; ctx.lineWidth = lw; ctx.stroke();
    }
    // text
    const midY = cy - r * 0.46;
    ctx.textAlign = 'center';
    ctx.fillStyle = isDark() ? '#e8e6e2' : '#0f1f2e';
    ctx.font = 'bold 19px "DM Sans", system-ui'; ctx.textBaseline = 'middle';
    ctx.fillText(Math.round(actualPct) + '%', cx, midY);
    ctx.fillStyle = isDark() ? '#a0a09a' : '#7a6e65';
    ctx.font = '11px "DM Sans", system-ui';
    ctx.fillText('occupancy', cx, midY + 20);
  }

  // ── Zero dashed line plugin ───────────────────────────────────────────────
  const zeroLinePlugin = {
    id: 'zeroLine',
    beforeDraw(chart: any) {
      const { ctx, chartArea, scales } = chart;
      if (!scales.y) return;
      const yZero = scales.y.getPixelForValue(0);
      if (yZero < chartArea.top || yZero > chartArea.bottom) return;
      ctx.save();
      ctx.beginPath(); ctx.setLineDash([4, 4]);
      ctx.strokeStyle = isDark() ? 'rgba(255,255,255,0.28)' : 'rgba(0,0,0,0.2)';
      ctx.lineWidth = 1;
      ctx.moveTo(chartArea.left, yZero); ctx.lineTo(chartArea.right, yZero); ctx.stroke();
      ctx.restore();
    },
  };

  // ── Visual results rendering ──────────────────────────────────────────────
  function renderVisuals(inputs: AnalysisInputs, r: ReturnType<typeof calcAll>) {

    // 1: Property header
    const prop = propertiesList.find(p => p.id === selectedPropertyId);
    document.getElementById('res-address')!.textContent = prop?.address_line1 || inpStr('inp-address') || '—';
    const br = inpStr('inp-bedrooms') || '—', ba = inpStr('inp-bathrooms') || '—', type = inpStr('inp-type') || '—';
    document.getElementById('res-subline')!.textContent = `${br} BR · ${ba} BA · ${type}${inputs.isHOA ? ' · HOA' : ''}`;

    const bufferPP = Math.round(inputs.occ - r.breakEvenOcc);
    const bufEl = document.getElementById('res-buffer')!;
    bufEl.textContent = (bufferPP >= 0 ? '+' : '') + bufferPP + 'pp';
    (bufEl as HTMLElement).style.color = bufferPP >= 0 ? C.green : C.red;

    const walkAway = inp('inp-walk-away');
    const badge = document.getElementById('res-badge')!;
    if (walkAway > 0 && inputs.rentNeg > walkAway) {
      const over = Math.round(inputs.rentNeg - walkAway);
      badge.className = 'res-badge res-badge--warn'; badge.style.display = '';
      badge.textContent = `⚠ Rent $${over.toLocaleString()} above walk-away threshold`;
    } else if (walkAway > 0) {
      badge.className = 'res-badge res-badge--ok'; badge.style.display = '';
      badge.textContent = '✓ Within target range';
    } else {
      badge.style.display = 'none';
    }

    // 2: Metric cards
    document.getElementById('res-net-monthly')!.textContent = '$' + Math.round(r.netMonthly).toLocaleString();
    document.getElementById('rmc-net-monthly')!.className = 'res-metric ' + (r.netMonthly > 700 ? 'rmc--green' : r.netMonthly >= 200 ? 'rmc--amber' : 'rmc--red');
    document.getElementById('res-margin-sub')!.textContent = Math.round(r.margin) + '% margin';

    document.getElementById('res-net-annual')!.textContent = '$' + Math.round(r.netAnnual).toLocaleString();
    document.getElementById('rmc-net-annual')!.className = 'res-metric ' + (r.netAnnual > 8400 ? 'rmc--green' : r.netAnnual >= 2400 ? 'rmc--amber' : 'rmc--red');

    document.getElementById('res-roi12')!.textContent = Math.round(r.roi12) + '%';
    document.getElementById('res-roi-sub')!.textContent = 'on $' + Math.round(r.totalInvest).toLocaleString() + ' invested';

    document.getElementById('res-payback')!.textContent = isFinite(r.payback) ? r.payback.toFixed(1) + ' mo' : '—';
    document.getElementById('res-mult-sub')!.textContent = r.multiple.toFixed(2) + 'x rent/rev';

    // 3a: Waterfall chart
    let floor = r.grossRevenue;
    const costItems = [
      { label: 'Rent',       val: inputs.rentNeg },
      { label: 'Utils',      val: inputs.utilities },
      { label: 'Internet',   val: inputs.internet },
      { label: 'Insurance',  val: inputs.insurance },
      { label: 'Supplies',   val: inputs.supplies },
      { label: 'Hospitable', val: inputs.pms + inputs.pricing },
      { label: 'Maint.',     val: r.maintenance },
      { label: 'Airbnb',     val: r.airbnbFee },
    ];
    const wfLabels: string[] = ['Gross'];
    const wfData: [number, number][] = [[0, r.grossRevenue]];
    const wfColors: string[] = [C.blue];
    costItems.forEach(({ label, val }) => {
      wfLabels.push(label);
      wfData.push([Math.max(0, floor - val), floor]);
      wfColors.push(C.red);
      floor -= val;
    });
    wfLabels.push('Net');
    wfColors.push(r.netMonthly >= 0 ? C.green : C.red);
    wfData.push(r.netMonthly >= 0 ? [0, r.netMonthly] : [r.netMonthly, 0]);

    if (!chartWaterfall) {
      const ctx2 = (document.getElementById('chart-waterfall') as HTMLCanvasElement).getContext('2d')!;
      chartWaterfall = new Chart(ctx2, {
        type: 'bar',
        data: { labels: wfLabels, datasets: [{ data: wfData as any, backgroundColor: wfColors, borderWidth: 0, borderRadius: 2 }] },
        options: {
          responsive: true, maintainAspectRatio: false,
          plugins: { legend: { display: false }, tooltip: { callbacks: { label: (c: any) => {
            const [mn, mx] = c.raw as [number, number];
            return '$' + Math.round(Math.abs(mx - mn)).toLocaleString();
          }}}},
          scales: {
            x: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 } } },
            y: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 }, callback: (v: any) => fmtK(v) } },
          },
        },
      });
    } else {
      const ds = chartWaterfall.data.datasets[0];
      ds.data = wfData as any; (ds as any).backgroundColor = wfColors;
      chartWaterfall.update();
    }

    // 3b: Gauge
    drawGauge(r.breakEvenOcc, inputs.occ);
    document.getElementById('gauge-legend')!.innerHTML =
      `<span class="gauge-dot" style="background:${C.red}"></span>Break-even ${Math.round(r.breakEvenOcc)}%` +
      `&emsp;<span class="gauge-dot" style="background:${C.green}"></span>Actual ${Math.round(inputs.occ)}%`;

    // 3c: Scenario bars
    const scData   = r.scenarioData.map(s => s.netAnnual);
    const scColors = [C.green, C.blue, C.red];
    if (!chartScenarios) {
      const ctx2 = (document.getElementById('chart-scenarios') as HTMLCanvasElement).getContext('2d')!;
      chartScenarios = new Chart(ctx2, {
        type: 'bar',
        data: { labels: ['Best', 'Base', 'Worst'], datasets: [{ data: scData, backgroundColor: scColors, borderWidth: 0, borderRadius: 2 }] },
        options: {
          indexAxis: 'y', responsive: true, maintainAspectRatio: false,
          plugins: { legend: { display: false } },
          scales: {
            x: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 }, callback: (v: any) => fmtK(v) } },
            y: { grid: { display: false }, ticks: { color: tickColor(), font: { size: 10 } } },
          },
        },
      });
    } else {
      chartScenarios.data.datasets[0].data = scData; chartScenarios.update();
    }

    // 4: Monthly cash flow
    const driver  = inpStr('inp-demand');
    const profile = getMonthlyProfile(driver, inputs.occ);
    const months  = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    const monthlyNets   = profile.occs.map(occ => occ * inputs.adr * 30 * 0.97 - r.fixed);
    const monthlyColors = monthlyNets.map(v => v >= 0 ? C.green : C.red);

    if (!chartMonthly) {
      const ctx2 = (document.getElementById('chart-monthly') as HTMLCanvasElement).getContext('2d')!;
      chartMonthly = new Chart(ctx2, {
        type: 'bar',
        data: { labels: months, datasets: [{ data: monthlyNets, backgroundColor: monthlyColors, borderWidth: 0, borderRadius: 2 }] },
        options: {
          responsive: true, maintainAspectRatio: false,
          plugins: { legend: { display: false } },
          scales: {
            x: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 } } },
            y: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 }, callback: (v: any) => fmtK(v) } },
          },
        },
        plugins: [zeroLinePlugin],
      });
    } else {
      const ds = chartMonthly.data.datasets[0];
      ds.data = monthlyNets; (ds as any).backgroundColor = monthlyColors;
      chartMonthly.update();
    }
    const occRange = Math.round((Math.max(...profile.occs) - Math.min(...profile.occs)) * 100);
    document.getElementById('monthly-footnote')!.textContent =
      `Based on ${profile.marketType} demand profile (${driver}). ±${occRange}pp occupancy variance applied monthly. Dashed line = break-even at $0.`;
    document.getElementById('market-note')!.textContent = profile.note;

    // 5: ROI curve
    const roiPoints = Array.from({ length: 25 }, (_, i) => -r.totalInvest + i * r.netMonthly);
    const roiLabels = Array.from({ length: 25 }, (_, i) => i % 2 === 0 ? `M${i}` : '');
    if (!chartRoi) {
      const ctx2 = (document.getElementById('chart-roi') as HTMLCanvasElement).getContext('2d')!;
      chartRoi = new Chart(ctx2, {
        type: 'line',
        data: { labels: roiLabels, datasets: [{ data: roiPoints, borderColor: C.blue, backgroundColor: 'rgba(55,138,221,0.12)', fill: true, tension: 0, pointRadius: 0, borderWidth: 2 }] },
        options: {
          responsive: true, maintainAspectRatio: false,
          plugins: { legend: { display: false } },
          scales: {
            x: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 } } },
            y: { grid: { color: gridColor() }, ticks: { color: tickColor(), font: { size: 10 }, callback: (v: any) => fmtK(v) } },
          },
        },
        plugins: [zeroLinePlugin],
      });
    } else {
      chartRoi.data.datasets[0].data = roiPoints; chartRoi.update();
    }
    document.getElementById('roi-footnote')!.textContent =
      `Dashed line = initial investment ($${Math.round(r.totalInvest).toLocaleString()}). Crossover = payback month. Base case assumptions.`;
  }

  // ── Calculations ─────────────────────────────────────────────────────────

  function gatherInputs(): AnalysisInputs {
    return {
      rentNeg:   inp('inp-rent-neg'),
      rentAsk:   inp('inp-rent-ask'),
      deposit:   inp('inp-deposit'),
      adr:       inp('inp-adr'),
      occ:       inp('inp-occ'),
      avgStay:   inp('inp-stay'),
      cleaning:  inp('inp-cleaning'),
      utilities: inp('inp-utilities'),
      internet:  inp('inp-internet'),
      insurance: inp('inp-insurance'),
      supplies:  inp('inp-supplies'),
      pms:       inp('inp-pms'),
      pricing:   inp('inp-pricing'),
      furniture: inp('inp-furniture'),
      photo:     inp('inp-photo'),
      lock:      inp('inp-lock'),
      legal:     inp('inp-legal'),
      misc:      inp('inp-misc'),
      isHOA:     inpStr('inp-hoa') === 'Yes',
    };
  }

  function runCalc() {
    const inputs = gatherInputs();
    const isHOA  = inputs.isHOA;

    document.getElementById('hoa-block')?.classList.toggle('visible', isHOA);
    document.getElementById('hoa-save-block')?.classList.toggle('visible', isHOA);

    const saveBtn   = document.getElementById('btn-save')   as HTMLButtonElement;
    const updateBtn = document.getElementById('btn-update') as HTMLButtonElement;
    const exportBtn = document.getElementById('btn-export') as HTMLButtonElement;
    saveBtn.disabled   = isHOA || !selectedPropertyId;
    updateBtn.disabled = isHOA;
    exportBtn.disabled = isHOA;

    const maintenance = calcMaintenance(inputs.rentNeg);
    const maintEl = document.getElementById('inp-maintenance') as HTMLInputElement;
    if (maintEl) maintEl.value = maintenance.toFixed(2);

    const r = calcAll(inputs);
    lastResults = r;

    const staysPerMonth = r.staysPerMonth;
    const cleaningTotal = staysPerMonth * inputs.cleaning;
    setOut('out-stays',              staysPerMonth.toFixed(1));
    setOut('out-cleaning-collected', fmt(cleaningTotal));
    setOut('out-cleaning-paid',      fmt(cleaningTotal));

    const flags  = getRiskFlags(inputs, r);
    const riskEl = document.getElementById('risk-flags')!;
    riskEl.innerHTML = flags.length === 0
      ? '<div class="risk-flag ok"><span class="risk-flag-icon">✓</span>No risk flags triggered.</div>'
      : flags.map(f => `<div class="risk-flag ${f.type}"><span class="risk-flag-icon">${f.icon}</span>${f.text}</div>`).join('');

    renderVisuals(inputs, r);
  }

  const inputIds = [
    'inp-name', 'inp-address',
    'inp-rent-ask', 'inp-rent-neg', 'inp-deposit', 'inp-hoa', 'inp-walk-away',
    'inp-adr', 'inp-occ', 'inp-stay', 'inp-demand',
    'inp-utilities', 'inp-internet', 'inp-insurance', 'inp-supplies', 'inp-pms', 'inp-pricing',
    'inp-cleaning',
    'inp-furniture', 'inp-photo', 'inp-lock', 'inp-legal', 'inp-misc',
    'inp-notes',
  ];
  inputIds.forEach(id => {
    document.getElementById(id)?.addEventListener('input', runCalc);
  });

  // ── Property picker ──────────────────────────────────────────────────────────

  async function loadProperties() {
    const { data, error } = await supabase
      .from('properties')
      .select('*, landlord:landlords(id, name)')
      .order('created_at', { ascending: false });

    propertiesList = data || [];
    const select = document.getElementById('inp-property') as HTMLSelectElement;

    if (error || !propertiesList.length) {
      select.innerHTML = '<option value="">— No properties yet —</option>';
      show('no-properties-hint');
      return;
    }

    hide('no-properties-hint');
    select.innerHTML = '<option value="">— Select a property —</option>' +
      propertiesList.map(p =>
        `<option value="${esc(p.id)}">${esc(p.address_line1)}, ${esc(p.city)}</option>`
      ).join('');

    if (selectedPropertyId) {
      select.value = selectedPropertyId;
    }
  }

  function applyPropertyMeta(prop: any | null) {
    const bedroomsEl  = document.getElementById('inp-bedrooms')  as HTMLInputElement;
    const bathroomsEl = document.getElementById('inp-bathrooms') as HTMLInputElement;
    const typeEl      = document.getElementById('inp-type')      as HTMLInputElement;
    if (!bedroomsEl || !bathroomsEl || !typeEl) return;
    if (prop) {
      bedroomsEl.value  = prop.bedrooms      != null ? String(prop.bedrooms)      : '';
      bathroomsEl.value = prop.bathrooms     != null ? String(prop.bathrooms)     : '';
      typeEl.value      = prop.property_type != null ? String(prop.property_type) : '';
    } else {
      bedroomsEl.value  = '';
      bathroomsEl.value = '';
      typeEl.value      = '';
    }
  }

  document.getElementById('inp-property')?.addEventListener('change', e => {
    const val = (e.target as HTMLSelectElement).value;
    selectedPropertyId = val || null;

    const prop = propertiesList.find(p => p.id === val);
    const addrEl = document.getElementById('inp-address') as HTMLInputElement;

    if (prop && addrEl) {
      const parts = [prop.address_line1];
      if (prop.address_line2) parts.push(prop.address_line2);
      parts.push(prop.city);
      addrEl.value = parts.join(', ');
    } else if (addrEl) {
      addrEl.value = '';
    }

    // Default label to address_line1 only if still showing the default placeholder
    const nameEl = document.getElementById('inp-name') as HTMLInputElement;
    if (prop && nameEl && nameEl.value.trim() === 'New Analysis') {
      nameEl.value = prop.address_line1;
    }

    applyPropertyMeta(prop ?? null);

    hide('orphan-warning');
    if (!selectedPropertyId) {
      show('no-property-hint');
    } else {
      hide('no-property-hint');
    }

    runCalc();
  });

  // ── Persistence ──────────────────────────────────────────────────────────────

  function gatherFormInputs() {
    const obj: Record<string, string> = {};
    inputIds.forEach(id => {
      const el = document.getElementById(id) as HTMLInputElement | null;
      if (el) obj[id] = el.value;
    });
    return obj;
  }

  function applyFormInputs(inputs: Record<string, string>) {
    Object.entries(inputs).forEach(([id, val]) => {
      const el = document.getElementById(id) as HTMLInputElement | null;
      if (el && !el.disabled && !el.readOnly) el.value = val;
    });
    runCalc();
  }

  function buildResults() {
    if (!lastResults) return {};
    const r = lastResults;
    return {
      grossRevenue: r.grossRevenue, airbnbFee: r.airbnbFee, netPlatform: r.netPlatform,
      fixed: r.fixed, maintenance: r.maintenance, staysPerMonth: r.staysPerMonth,
      netMonthly: r.netMonthly, netAnnual: r.netAnnual, breakEvenOcc: r.breakEvenOcc,
      margin: r.margin, multiple: r.multiple, totalInvest: r.totalInvest,
      payback: r.payback, roi12: r.roi12, roi24: r.roi24,
    };
  }

  async function loadAnalysisById(id: string) {
    const { data, error } = await supabase
      .from('property_analyses')
      .select('*')
      .eq('id', id)
      .single();
    if (error || !data) return;

    currentAnalysisId = id;
    selectedPropertyId = data.property_id || null;
    applyFormInputs(data.inputs);

    const propSelect = document.getElementById('inp-property') as HTMLSelectElement;
    if (propSelect) propSelect.value = selectedPropertyId ?? '';

    const addrEl = document.getElementById('inp-address') as HTMLInputElement;
    if (addrEl && data.address) addrEl.value = data.address;

    applyPropertyMeta(propertiesList.find(p => p.id === selectedPropertyId) ?? null);

    if (!data.property_id) {
      show('orphan-warning');
      hide('no-property-hint');
    } else {
      hide('orphan-warning');
      hide('no-property-hint');
    }

    (document.getElementById('btn-save')   as HTMLElement).style.display = 'none';
    (document.getElementById('btn-update') as HTMLElement).style.display = '';
    (document.getElementById('btn-new')    as HTMLElement).style.display = '';
    history.replaceState(null, '', '/admin/analyzer');
    runCalc();
  }

  async function saveAnalysis() {
    if (!selectedPropertyId) return;
    const btn = document.getElementById('btn-save') as HTMLButtonElement;
    btn.disabled = true; btn.textContent = 'Saving…';

    const prop = propertiesList.find(p => p.id === selectedPropertyId);
    const address = prop
      ? [prop.address_line1, prop.address_line2, prop.city].filter(Boolean).join(', ')
      : inpStr('inp-address');

    const { data, error } = await supabase
      .from('property_analyses')
      .insert([{
        name:        inpStr('inp-name') || 'New Analysis',
        address,
        property_id: selectedPropertyId,
        inputs:      gatherFormInputs(),
        results:     buildResults(),
        notes:       inpStr('inp-notes'),
      }])
      .select().single();

    btn.disabled = false;
    btn.innerHTML = `<svg viewBox="0 0 24 24"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg> Save Analysis`;

    if (error) { alert('Save failed: ' + error.message); return; }
    currentAnalysisId = data.id;
    (document.getElementById('btn-save')   as HTMLElement).style.display = 'none';
    (document.getElementById('btn-update') as HTMLElement).style.display = '';
    (document.getElementById('btn-new')    as HTMLElement).style.display = '';
  }

  async function updateAnalysis() {
    if (!currentAnalysisId) return;
    const btn = document.getElementById('btn-update') as HTMLButtonElement;
    btn.disabled = true; btn.textContent = 'Updating…';

    const prop = propertiesList.find(p => p.id === selectedPropertyId);
    const address = prop
      ? [prop.address_line1, prop.address_line2, prop.city].filter(Boolean).join(', ')
      : inpStr('inp-address');

    const { error } = await supabase
      .from('property_analyses')
      .update({
        name:        inpStr('inp-name') || 'New Analysis',
        address,
        property_id: selectedPropertyId,
        inputs:      gatherFormInputs(),
        results:     buildResults(),
        notes:       inpStr('inp-notes'),
        updated_at:  new Date().toISOString(),
      })
      .eq('id', currentAnalysisId);

    btn.disabled = false;
    btn.innerHTML = `<svg viewBox="0 0 24 24"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg> Update`;
    if (error) alert('Update failed: ' + error.message);
  }

  function resetForm() {
    currentAnalysisId = null;
    selectedPropertyId = null;
    hide('orphan-warning');
    hide('no-property-hint');

    const defaults: Record<string, string> = {
      'inp-name': 'New Analysis', 'inp-address': '',
      'inp-rent-ask': '1400', 'inp-rent-neg': '1200', 'inp-deposit': '1400', 'inp-hoa': 'No',
      'inp-adr': '167', 'inp-occ': '77', 'inp-stay': '3.2', 'inp-demand': 'Military TDY',
      'inp-walk-away': '1750',
      'inp-utilities': '185', 'inp-internet': '80', 'inp-insurance': '100',
      'inp-supplies': '120', 'inp-pms': '39', 'inp-pricing': '0',
      'inp-cleaning': '120',
      'inp-furniture': '6500', 'inp-photo': '200', 'inp-lock': '150',
      'inp-legal': '300', 'inp-misc': '500', 'inp-notes': '',
    };
    Object.entries(defaults).forEach(([id, val]) => {
      const el = document.getElementById(id) as HTMLInputElement | null;
      if (el) el.value = val;
    });
    const propSelect = document.getElementById('inp-property') as HTMLSelectElement;
    if (propSelect) propSelect.value = '';

    applyPropertyMeta(null);

    (document.getElementById('btn-save')   as HTMLElement).style.display = '';
    (document.getElementById('btn-update') as HTMLElement).style.display = 'none';
    (document.getElementById('btn-new')    as HTMLElement).style.display = 'none';
    show('no-property-hint');
    runCalc();
  }

  // ── Export to Markdown ───────────────────────────────────────────────────────

  function exportMarkdown() {
    if (!lastResults) return;
    const r = lastResults;
    const name  = inpStr('inp-name') || 'New Analysis';
    const date  = new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
    const flags = Array.from(document.querySelectorAll('#risk-flags .risk-flag'))
      .map(f => f.textContent?.trim() ?? '')
      .filter(t => !t.startsWith('✓'))
      .map(t => '- ' + t);

    const mdTotalCosts  = r.airbnbFee + r.fixed;
    const mdStays       = r.staysPerMonth;
    const mdClean       = mdStays * inp('inp-cleaning');
    const mdInternetLine = inp('inp-internet') > 0
      ? `| Internet | ${fmt(inp('inp-internet'))} |\n` : '';

    const md = `# Property Analysis — ${name}
**R&L Capital Realty LLC** | Abilene, TX | Generated: ${date}

## Property Details
- Address: ${inpStr('inp-address') || 'N/A'}
- Bedrooms: ${inpStr('inp-bedrooms')} | Bathrooms: ${inpStr('inp-bathrooms')}
- Property Type: ${inpStr('inp-type')}
- Negotiated Rent: ${fmt(inp('inp-rent-neg'))}

## Revenue Assumptions
- ADR: ${fmt(inp('inp-adr'))} | Occupancy: ${inp('inp-occ')}% | Avg Stay: ${inp('inp-stay')} nights
- Estimated stays/month: ${mdStays.toFixed(1)}

## Monthly P&L — Base Case
| Metric | Amount |
|--------|--------|
| Gross Revenue | ${fmt(r.grossRevenue)} |
| **Monthly Costs** | |
| Rent | ${fmt(inp('inp-rent-neg'))} |
| Utilities | ${fmt(inp('inp-utilities'))} |
${mdInternetLine}| Insurance (Proper STR) | ${fmt(inp('inp-insurance'))} |
| Supplies & Amenities | ${fmt(inp('inp-supplies'))} |
| Hospitable PMS | ${fmt(inp('inp-pms'))} |
| PriceLabs | ${fmt(inp('inp-pricing'))} |
| Maintenance Reserve | ${fmt(r.maintenance)} |
| Airbnb Fee (3%) | ${fmt(r.airbnbFee)} |
| STR Permit | $0 |
| **Total Costs** | **${fmt(mdTotalCosts)}** |
| Net Monthly Profit | ${fmt(r.netMonthly)} |
| Net Annual Profit | ${fmt(r.netAnnual)} |
| Break-Even Occupancy | ${fmtPct(r.breakEvenOcc)} |
| Margin % | ${fmtPct(r.margin)} |
| Rent-to-Revenue Multiple | ${fmtX(r.multiple)} |

## Cleaning — Pass-Through
| | |
|-|-|
| Stays / Month | ${mdStays.toFixed(1)} |
| Cleaning Collected (from guest) | ${fmt(mdClean)} |
| Cleaning Paid (to cleaner) | ${fmt(mdClean)} |
| Net P&L Impact | $0 |

## Scenario Analysis
| Scenario | Occ% | ADR | Net/mo | Net/yr |
|----------|------|-----|--------|--------|
${r.scenarioData.map(s => `| ${s.label.charAt(0).toUpperCase() + s.label.slice(1)} | ${fmtPct(s.occ)} | ${fmt(s.adr)} | ${fmt(s.netMonthly)} | ${fmt(s.netAnnual)} |`).join('\n')}

## Investment Summary
| Metric | Value |
|--------|-------|
| Total Initial Investment | ${fmt(r.totalInvest)} |
| Payback Period | ${isFinite(r.payback) ? r.payback.toFixed(1) + ' months' : '—'} |
| 12-Month ROI | ${fmtPct(r.roi12)} |
| 24-Month ROI | ${fmtPct(r.roi24)} |

## Risk Assessment
${flags.length ? flags.join('\n') : 'No risk flags triggered.'}

## Abilene Market Notes
- STR permit: Not required (City of Abilene, 2026)
- HOT tax: Collected and remitted by Airbnb — no operator action required
- Primary demand driver: Dyess Air Force Base (TDY military) + ACU / Hardin-Simmons / McMurry
- Insurance: Proper Insurance STR policy (~$98–103/mo), landlord named as additional insured
- Sublease: Requires explicit addendum — Texas RE attorney review recommended (~$300)

## Notes
${inpStr('inp-notes') || 'None.'}
`;
    const blob = new Blob([md], { type: 'text/markdown' });
    const url  = URL.createObjectURL(blob);
    const a    = document.createElement('a');
    a.href = url;
    const _ts  = new Date();
    const _pad = (n: number) => String(n).padStart(2, '0');
    const _stamp = `${_ts.getFullYear()}${_pad(_ts.getMonth()+1)}${_pad(_ts.getDate())}_${_pad(_ts.getHours())}${_pad(_ts.getMinutes())}${_pad(_ts.getSeconds())}`;
    a.download = (name.replace(/[^a-z0-9]/gi, '_').toLowerCase() || 'analysis') + '-' + _stamp + '.md';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  }

  // ── Button wiring ────────────────────────────────────────────────────────────

  document.getElementById('btn-save')!.addEventListener('click', saveAnalysis);
  document.getElementById('btn-update')!.addEventListener('click', updateAnalysis);
  document.getElementById('btn-export')!.addEventListener('click', exportMarkdown);
  document.getElementById('btn-new')!.addEventListener('click', () => {
    resetForm();
    loadProperties();
  });

  // ── New Property Modal ────────────────────────────────────────────────────────

  let landlordsList: any[] = [];

  async function loadLandlordsForModal() {
    const { data } = await supabase.from('landlords').select('id, name').order('name');
    landlordsList = data || [];
    const sel = document.getElementById('np-landlord') as HTMLSelectElement;
    if (!landlordsList.length) {
      sel.innerHTML = '<option value="">— No landlords —</option>';
      show('no-landlords-msg');
    } else {
      hide('no-landlords-msg');
      sel.innerHTML = '<option value="">— Select landlord —</option>' +
        landlordsList.map(l => `<option value="${esc(l.id)}">${esc(l.name)}</option>`).join('');
    }
  }

  function openModal() {
    // Clear form
    ['np-addr1','np-addr2','np-city','np-zip','np-neighborhood','np-notes'].forEach(id => {
      (document.getElementById(id) as HTMLInputElement).value = '';
    });
    (document.getElementById('np-landlord') as HTMLSelectElement).value = '';
    hide('modal-error');
    hide('quick-landlord-form');
    (document.getElementById('btn-toggle-ql') as HTMLButtonElement).textContent = '';
    const qlToggle = document.getElementById('btn-toggle-ql')!;
    qlToggle.innerHTML = `<svg viewBox="0 0 24 24" width="12" height="12" stroke="currentColor" fill="none" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg> Quick-create landlord`;

    loadLandlordsForModal();

    document.getElementById('modal-backdrop')!.style.display = '';
    document.getElementById('modal-new-property')!.style.display = '';
  }

  function closeModal() {
    hide('modal-backdrop');
    hide('modal-new-property');
  }

  document.getElementById('btn-new-property')?.addEventListener('click', openModal);
  document.getElementById('modal-close')?.addEventListener('click', closeModal);
  document.getElementById('btn-modal-cancel')?.addEventListener('click', closeModal);
  document.getElementById('modal-backdrop')?.addEventListener('click', closeModal);

  // Quick-create landlord toggle
  let qlVisible = false;
  document.getElementById('btn-toggle-ql')?.addEventListener('click', () => {
    qlVisible = !qlVisible;
    const form = document.getElementById('quick-landlord-form')!;
    form.style.display = qlVisible ? '' : 'none';
    const btn = document.getElementById('btn-toggle-ql')!;
    btn.innerHTML = qlVisible
      ? `<svg viewBox="0 0 24 24" width="12" height="12" stroke="currentColor" fill="none" stroke-width="2"><line x1="5" y1="12" x2="19" y2="12"/></svg> Cancel`
      : `<svg viewBox="0 0 24 24" width="12" height="12" stroke="currentColor" fill="none" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg> Quick-create landlord`;
  });

  document.getElementById('btn-create-landlord')?.addEventListener('click', async () => {
    const name  = (document.getElementById('ql-name')  as HTMLInputElement).value.trim();
    const phone = (document.getElementById('ql-phone') as HTMLInputElement).value.trim();
    const email = (document.getElementById('ql-email') as HTMLInputElement).value.trim();
    const type  = (document.getElementById('ql-type')  as HTMLSelectElement).value;
    if (!name || !phone || !email || !type) { alert('Fill in all required landlord fields.'); return; }

    const btn = document.getElementById('btn-create-landlord') as HTMLButtonElement;
    btn.disabled = true; btn.textContent = 'Creating…';

    const { data: { session } } = await supabase.auth.getSession();
    const { data, error } = await supabase
      .from('landlords')
      .insert([{ name, phone, email, type, user_id: session?.user.id }])
      .select().single();

    btn.disabled = false; btn.textContent = 'Create Landlord';
    if (error) { alert('Failed to create landlord: ' + error.message); return; }

    landlordsList.push(data);
    const sel = document.getElementById('np-landlord') as HTMLSelectElement;
    sel.innerHTML = '<option value="">— Select landlord —</option>' +
      landlordsList.map(l => `<option value="${esc(l.id)}">${esc(l.name)}</option>`).join('');
    sel.value = data.id;
    hide('no-landlords-msg');

    // Reset quick-create form and hide it
    ['ql-name','ql-phone','ql-email'].forEach(id => (document.getElementById(id) as HTMLInputElement).value = '');
    (document.getElementById('ql-type') as HTMLSelectElement).value = '';
    qlVisible = false;
    hide('quick-landlord-form');
    const toggleBtn = document.getElementById('btn-toggle-ql')!;
    toggleBtn.innerHTML = `<svg viewBox="0 0 24 24" width="12" height="12" stroke="currentColor" fill="none" stroke-width="2"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg> Quick-create landlord`;
  });

  document.getElementById('btn-modal-save')?.addEventListener('click', async () => {
    const addr1        = (document.getElementById('np-addr1')        as HTMLInputElement).value.trim();
    const addr2        = (document.getElementById('np-addr2')        as HTMLInputElement).value.trim();
    const city         = (document.getElementById('np-city')         as HTMLInputElement).value.trim();
    const zip          = (document.getElementById('np-zip')          as HTMLInputElement).value.trim();
    const neighborhood = (document.getElementById('np-neighborhood') as HTMLInputElement).value.trim();
    const notes        = (document.getElementById('np-notes')        as HTMLTextAreaElement).value.trim();
    const landlordId   = (document.getElementById('np-landlord')     as HTMLSelectElement).value;
    const npBedrooms   = (document.getElementById('np-bedrooms')     as HTMLSelectElement).value;
    const npBathrooms  = (document.getElementById('np-bathrooms')    as HTMLSelectElement).value;
    const npType       = (document.getElementById('np-type')         as HTMLSelectElement).value;

    const errEl = document.getElementById('modal-error')!;
    if (!addr1 || !city || !zip || !landlordId) {
      errEl.textContent = 'Address line 1, city, zip code, and landlord are required.';
      errEl.style.display = '';
      return;
    }
    hide('modal-error');

    const btn = document.getElementById('btn-modal-save') as HTMLButtonElement;
    btn.disabled = true; btn.textContent = 'Creating…';

    const { data: { session } } = await supabase.auth.getSession();
    const { data, error } = await supabase
      .from('properties')
      .insert([{
        address_line1: addr1,
        address_line2: addr2 || null,
        city,
        zip_code: zip,
        neighborhood: neighborhood || null,
        notes: notes || null,
        landlord_id: landlordId,
        user_id: session?.user.id,
        bedrooms:      npBedrooms  ? parseInt(npBedrooms)       : null,
        bathrooms:     npBathrooms ? parseFloat(npBathrooms)    : null,
        property_type: npType      || null,
      }])
      .select('*, landlord:landlords(id, name)')
      .single();

    btn.disabled = false; btn.textContent = 'Create Property';

    if (error) {
      errEl.textContent = 'Failed to create property: ' + error.message;
      errEl.style.display = '';
      return;
    }

    closeModal();
    propertiesList.unshift(data);

    const select = document.getElementById('inp-property') as HTMLSelectElement;
    hide('no-properties-hint');
    select.innerHTML = '<option value="">— Select a property —</option>' +
      propertiesList.map(p =>
        `<option value="${esc(p.id)}">${esc(p.address_line1)}, ${esc(p.city)}</option>`
      ).join('');

    // Auto-select the newly created property
    select.value = data.id;
    selectedPropertyId = data.id;
    const addrEl = document.getElementById('inp-address') as HTMLInputElement;
    if (addrEl) {
      const parts = [data.address_line1];
      if (data.address_line2) parts.push(data.address_line2);
      parts.push(data.city);
      addrEl.value = parts.join(', ');
    }
    applyPropertyMeta(data);
    hide('orphan-warning');
    hide('no-property-hint');
    runCalc();
  });

  // ── Init ─────────────────────────────────────────────────────────────────────

  (async () => {
    const session = await requireAuth();
    if (!session) return;

    runCalc();
    await loadProperties();

    // Check for ?load= URL param (navigated from Saved Analyses)
    const loadId = new URLSearchParams(location.search).get('load');
    if (loadId) {
      await loadAnalysisById(loadId);
    } else {
      // Check for property pre-selection from Properties page "New Analysis" shortcut
      const preselectedId   = sessionStorage.getItem('analyzer_property_id');
      const preselectedAddr = sessionStorage.getItem('analyzer_property_addr');
      if (preselectedId) {
        sessionStorage.removeItem('analyzer_property_id');
        sessionStorage.removeItem('analyzer_property_addr');
        selectedPropertyId = preselectedId;
        const propSelect = document.getElementById('inp-property') as HTMLSelectElement;
        if (propSelect) propSelect.value = preselectedId;
        const addrEl = document.getElementById('inp-address') as HTMLInputElement;
        if (addrEl && preselectedAddr) addrEl.value = preselectedAddr;
        applyPropertyMeta(propertiesList.find(p => p.id === preselectedId) ?? null);
        hide('orphan-warning');
        hide('no-property-hint');
        runCalc();
      } else {
        show('no-property-hint');
      }
    }

    document.body.classList.add('ready');
  })();
</script>

<style>
  body { font-family: 'DM Sans', system-ui, sans-serif; background: #f6f5f3; color: #0f1f2e; font-size: 16px; line-height: 1.6; min-height: 100vh; overflow-x: hidden; opacity: 0; transition: opacity .4s ease; font-optical-sizing: auto; }
  body.ready { opacity: 1; }

  .page { min-height: 100vh; position: relative; z-index: 1; }
  .portal-content { padding: 48px 5vw 80px; max-width: 1200px; margin: 0 auto; }
  .section-label { font-family: 'DM Sans', system-ui, sans-serif; font-size: .65rem; font-weight: 500; letter-spacing: .22em; text-transform: uppercase; color: #a8885a; display: flex; align-items: center; gap: 12px; margin-bottom: 32px; }
  .section-label::before { content: ''; display: block; width: 28px; height: 1px; background: #a8885a; }

  .calc-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; align-items: start; }
  .calc-panel { background: #ffffff; border: 1px solid rgba(15,31,46,.1); box-shadow: 0 1px 4px rgba(15,31,46,.06); padding: 32px 28px; }
  .calc-panel-title { font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.15rem; font-weight: 500; color: #0f1f2e; margin-bottom: 24px; letter-spacing: .01em; padding-bottom: 14px; border-bottom: 1px solid rgba(15,31,46,.08); display: flex; justify-content: space-between; align-items: center; }

  /* Property Association */
  .property-assoc { padding: 0 0 18px; margin-bottom: 4px; border-bottom: 1px solid rgba(15,31,46,.08); }
  .property-assoc-row { display: flex; align-items: flex-end; gap: 10px; margin-bottom: 8px; }
  .btn-add-prop {
    display: inline-flex; align-items: center; gap: 5px; flex-shrink: 0;
    padding: 9px 14px; border: 1px solid rgba(15,31,46,.2); border-radius: 2px;
    font-family: 'DM Sans', system-ui, sans-serif; font-size: .72rem; font-weight: 400;
    letter-spacing: .08em; text-transform: uppercase; color: rgba(15,31,46,.6);
    background: none; cursor: pointer; transition: border-color .2s, color .2s, background .2s;
    white-space: nowrap;
  }
  .btn-add-prop:hover { border-color: #c9a96e; color: #a8885a; }
  .btn-add-prop svg { width: 11px; height: 11px; stroke: currentColor; fill: none; stroke-width: 2; stroke-linecap: round; }
  .assoc-banner {
    font-size: .75rem; padding: 8px 12px; border-radius: 2px; line-height: 1.5; margin-top: 6px;
  }
  .assoc-banner a { color: inherit; font-weight: 500; text-decoration: underline; }
  .assoc-banner.warn { background: rgba(212,163,42,.1); border: 1px solid rgba(212,163,42,.3); color: rgba(180,130,0,.9); }
  .assoc-banner.info { background: rgba(201,169,110,.07); border: 1px solid rgba(201,169,110,.2); color: rgba(15,31,46,.55); }

  .calc-section { border-bottom: 1px solid rgba(15,31,46,.08); }
  .calc-section:last-child { border-bottom: none; }
  .calc-section-toggle { width: 100%; display: flex; align-items: center; justify-content: space-between; padding: 14px 0; background: rgba(15,31,46,.02); border: none; cursor: pointer; font-family: 'DM Sans', system-ui, sans-serif; font-size: .72rem; font-weight: 500; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; transition: color .35s; }
  .calc-section-toggle:hover { color: #c9a96e; }
  .calc-section-toggle svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; transition: transform .25s ease; flex-shrink: 0; }
  .calc-section-toggle.open svg { transform: rotate(180deg); }
  .calc-section-body { display: none; padding-bottom: 16px; }
  .calc-section-body.open { display: block; }

  .field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 10px; }
  .field-row.full { grid-template-columns: 1fr; }
  .field-group { display: flex; flex-direction: column; gap: 4px; }
  label { font-size: .68rem; letter-spacing: .1em; text-transform: uppercase; color: #7a6e65; font-weight: 400; }
  .req { color: #c94a4a; font-style: normal; }
  input[type="text"], input[type="number"], input[type="email"], select, textarea { background: #ffffff; border: 1px solid rgba(15,31,46,.15); border-radius: 2px; padding: 9px 12px; font-family: 'DM Sans', system-ui, sans-serif; font-size: .88rem; color: #0f1f2e; width: 100%; transition: border-color .35s, background .35s; -webkit-appearance: none; appearance: none; }
  input:focus, select:focus, textarea:focus { outline: none; border-color: #c9a96e; background: rgba(201,169,110,.04); }
  input[readonly], input[disabled] { opacity: .5; cursor: not-allowed; background: rgba(15,31,46,.03); }
  input.prop-auto-field { opacity: 1; cursor: default; color: rgba(15,31,46,.6); font-style: italic; }
  input::placeholder, textarea::placeholder { color: #8a7e78; }
  select option { background: #ffffff; color: #0f1f2e; }
  textarea { resize: vertical; min-height: 72px; line-height: 1.5; }
  .field-note { font-size: .72rem; color: #7a6e65; line-height: 1.4; font-style: italic; margin-top: 2px; }
  .field-badge { display: inline-flex; align-items: center; gap: 6px; padding: 3px 8px; border: 1px solid rgba(201,169,110,.2); border-radius: 2px; font-size: .65rem; letter-spacing: .1em; text-transform: uppercase; color: rgba(201,169,110,.6); font-style: normal; }

  .hoa-block { display: none; background: rgba(201,74,74,.12); border: 2px solid #c94a4a; border-radius: 2px; padding: 16px 18px; margin-top: 12px; }
  .hoa-block.visible { display: block; }
  .hoa-block-title { font-family: 'DM Sans', system-ui, sans-serif; font-size: .8rem; font-weight: 500; letter-spacing: .08em; text-transform: uppercase; color: #c94a4a; margin-bottom: 6px; display: flex; align-items: center; gap: 8px; }
  .hoa-block-body { font-size: .88rem; color: rgba(201,74,74,.9); line-height: 1.6; }
  .hot-info { background: rgba(201,169,110,.05); border: 1px solid rgba(201,169,110,.15); border-radius: 2px; padding: 14px 16px; margin-top: 8px; }
  .hot-info p { font-size: .88rem; color: #7a6e65; line-height: 1.65; }
  .hot-info strong { color: rgba(15,31,46,.7); font-weight: 400; }

  .output-panel { background: #ffffff; border: 1px solid rgba(15,31,46,.1); box-shadow: 0 1px 4px rgba(15,31,46,.06); padding: 24px 22px; display: flex; flex-direction: column; gap: 18px; }
  .out-section-title { font-family: 'DM Sans', system-ui, sans-serif; font-size: .65rem; font-weight: 500; letter-spacing: .18em; text-transform: uppercase; color: #a8885a; margin-bottom: 14px; padding-bottom: 8px; border-bottom: 1px solid rgba(201,169,110,.08); }
  .metric-row { display: flex; justify-content: space-between; align-items: baseline; padding: 5px 0; border-bottom: 1px solid rgba(15,31,46,.06); font-size: .83rem; gap: 8px; }
  .metric-row:last-child { border-bottom: none; }
  .metric-label { color: #7a6e65; font-weight: 300; flex-shrink: 0; }
  .metric-value { font-weight: 400; letter-spacing: .02em; text-align: right; color: #0f1f2e; }
  .metric-value.green { color: #4caf81; }
  .metric-value.amber { color: #d4a32a; }
  .metric-value.red   { color: #c94a4a; }
  .metric-row.highlight { background: rgba(15,31,46,.04); margin: 0 -8px; padding: 7px 8px; border-bottom: 1px solid rgba(15,31,46,.06); }
  .metric-row.highlight .metric-label { color: rgba(15,31,46,.65); font-weight: 400; }
  .metric-row.highlight .metric-value { font-size: .95rem; }
  .metric-row--gross { padding-bottom: 10px; }
  .cost-stack-label { font-size: .62rem; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; font-weight: 500; padding: 8px 0 4px; border-top: 1px solid rgba(201,169,110,.12); margin-top: 4px; }
  .metric-row.cost-item { padding: 4px 0 4px 10px; font-size: .8rem; border-bottom: 1px solid rgba(15,31,46,.04); }
  .metric-row.cost-item .metric-label { color: rgba(15,31,46,.55); font-weight: 300; }
  .metric-row--total { border-top: 1px solid rgba(15,31,46,.12); margin-top: 2px; padding-top: 7px; font-weight: 500; }
  .metric-row--total .metric-label { color: rgba(15,31,46,.75); font-weight: 500; }
  .passthrough-note { font-size: .75rem; color: #7a6e65; font-style: italic; margin-bottom: 10px; line-height: 1.5; }

  .scenario-table { width: 100%; border-collapse: collapse; font-size: .8rem; margin-top: 8px; }
  .scenario-table th { font-size: .68rem; letter-spacing: .12em; text-transform: uppercase; color: #7a6e65; font-weight: 400; padding: 12px 16px; text-align: right; border-bottom: 1px solid rgba(15,31,46,.08); }
  .scenario-table th:first-child { text-align: left; }
  .scenario-table td { padding: 11px 16px; font-size: .92rem; color: #0f1f2e; border-bottom: 1px solid rgba(15,31,46,.08); text-align: right; }
  .scenario-table td:first-child { text-align: left; color: rgba(15,31,46,.5); font-size: .75rem; letter-spacing: .06em; text-transform: uppercase; }
  .scenario-table tr.worst-negative td { background: rgba(201,74,74,.08); color: #c94a4a; }
  .scenario-table tr.scenario-best td:not(:first-child) { color: #4caf81; }

  .risk-flags { display: flex; flex-direction: column; gap: 7px; }
  .risk-flag { display: flex; align-items: flex-start; gap: 10px; padding: 10px 12px; border-radius: 2px; font-size: .8rem; line-height: 1.5; }
  .risk-flag.warn   { background: rgba(212,163,42,.1); border: 1px solid rgba(212,163,42,.25); color: rgba(212,163,42,.9); }
  .risk-flag.danger { background: rgba(201,74,74,.1); border: 1px solid rgba(201,74,74,.3); color: rgba(201,74,74,.9); }
  .risk-flag.ok     { background: rgba(76,175,129,.1); border: 1px solid rgba(76,175,129,.2); color: rgba(76,175,129,.9); font-size: .75rem; }
  .risk-flag-icon { flex-shrink: 0; font-size: 1rem; line-height: 1; }

  .calc-actions { display: flex; gap: 10px; flex-wrap: wrap; padding-top: 16px; border-top: 1px solid rgba(201,169,110,.1); }
  .btn { display: inline-flex; align-items: center; gap: 8px; padding: 10px 20px; border-radius: 2px; cursor: pointer; font-family: 'DM Sans', system-ui, sans-serif; font-size: .75rem; font-weight: 400; letter-spacing: .1em; text-transform: uppercase; transition: all .35s; border: 1px solid; }
  .btn-primary { background: #c9a96e; color: #0f1f2e; border-color: #c9a96e; }
  .btn-primary:hover { background: #dfc090; border-color: #dfc090; }
  .btn-primary:disabled { opacity: .4; cursor: not-allowed; }
  .btn-ghost { background: none; color: rgba(15,31,46,.7); border-color: rgba(15,31,46,.25); }
  .btn-ghost:hover { border-color: #0f1f2e; color: #0f1f2e; }
  .btn-ghost:disabled { opacity: .4; cursor: not-allowed; }
  .btn-sm { padding: 7px 14px; font-size: .7rem; }
  .btn svg { width: 13px; height: 13px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }

  .view-saved-link { padding-top: 4px; text-align: center; }
  .view-saved-link a { font-size: .72rem; color: #a8885a; letter-spacing: .04em; text-decoration: none; transition: color .2s; }
  .view-saved-link a:hover { color: #c9a96e; }

  /* Modal */
  .modal-backdrop { position: fixed; inset: 0; background: rgba(15,31,46,.35); z-index: 200; }
  .modal {
    position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%);
    width: 480px; max-width: calc(100vw - 32px); max-height: 90vh; overflow-y: auto;
    background: #ffffff; border: 1px solid rgba(15,31,46,.12);
    box-shadow: 0 8px 40px rgba(15,31,46,.18); z-index: 201; border-radius: 3px;
  }
  .modal-header { display: flex; align-items: center; justify-content: space-between; padding: 22px 24px 16px; border-bottom: 1px solid rgba(15,31,46,.08); }
  .modal-title { font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.1rem; font-weight: 500; color: #0f1f2e; }
  .modal-close { background: none; border: none; cursor: pointer; font-size: 1.4rem; color: rgba(15,31,46,.4); line-height: 1; padding: 4px 8px; transition: color .2s; }
  .modal-close:hover { color: #0f1f2e; }
  .modal-body { padding: 20px 24px 24px; display: flex; flex-direction: column; gap: 12px; }
  .modal-error { background: rgba(201,74,74,.1); border: 1px solid rgba(201,74,74,.3); color: #c94a4a; padding: 10px 14px; border-radius: 2px; font-size: .82rem; }
  .modal-actions { display: flex; gap: 10px; justify-content: flex-end; padding-top: 8px; border-top: 1px solid rgba(15,31,46,.08); margin-top: 4px; }

  /* Quick-create landlord */
  .quick-landlord-toggle { margin-top: -4px; }
  .link-btn { background: none; border: none; cursor: pointer; display: inline-flex; align-items: center; gap: 5px; font-family: 'DM Sans', system-ui, sans-serif; font-size: .72rem; color: #a8885a; letter-spacing: .04em; padding: 0; transition: color .2s; }
  .link-btn:hover { color: #c9a96e; }
  .quick-landlord-form { background: rgba(201,169,110,.05); border: 1px solid rgba(201,169,110,.15); border-radius: 2px; padding: 16px; display: flex; flex-direction: column; gap: 10px; margin-top: 4px; }
  .ql-title { font-size: .65rem; letter-spacing: .16em; text-transform: uppercase; color: #a8885a; font-weight: 500; margin-bottom: 2px; }

  footer { border-top: 1px solid rgba(201,169,110,.1); padding: 24px 5vw; display: flex; align-items: center; justify-content: space-between; gap: 16px; flex-wrap: wrap; position: relative; z-index: 1; }
  .footer-brand { font-family: 'Cormorant Garamond', Georgia, serif; font-size: .95rem; color: rgba(15,31,46,.65); letter-spacing: .04em; }
  .footer-brand span { color: #a8885a; }
  .footer-meta { font-size: .72rem; color: #7a6e65; letter-spacing: .06em; }

  /* ── Visual results panel ──────────────────────────────────────────────── */
  .res-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; padding-bottom: 14px; border-bottom: 1px solid rgba(15,31,46,.08); }
  .res-header-main { min-width: 0; }
  .res-address { font-family: 'DM Sans', system-ui, sans-serif; font-size: .95rem; font-weight: 500; color: #0f1f2e; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .res-subline { font-size: .72rem; color: #7a6e65; margin-top: 3px; letter-spacing: .02em; }
  .res-header-side { text-align: right; flex-shrink: 0; }
  .res-buffer-label { font-size: .62rem; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; font-weight: 500; }
  .res-buffer-val { font-size: 1.15rem; font-weight: 500; letter-spacing: -.01em; margin-top: 2px; }

  .res-badge { font-size: .75rem; padding: 6px 12px; border-radius: 2px; line-height: 1.4; font-weight: 400; }
  .res-badge--ok   { background: #EAF3DE; color: #27500A; border: 1px solid rgba(39,80,10,.2); }
  .res-badge--warn { background: #FAEEDA; color: #633806; border: 1px solid rgba(99,56,6,.2); }

  .res-metrics { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; }
  .res-metric { background: rgba(15,31,46,.03); border: 1px solid rgba(15,31,46,.08); border-radius: 2px; padding: 12px 10px 10px; text-align: center; transition: border-color .2s; }
  .res-metric.rmc--green { background: rgba(29,158,117,.06); border-color: rgba(29,158,117,.2); }
  .res-metric.rmc--amber { background: rgba(212,163,42,.06); border-color: rgba(212,163,42,.2); }
  .res-metric.rmc--red   { background: rgba(226,75,74,.06);  border-color: rgba(226,75,74,.2); }
  .rmc-val   { font-size: .98rem; font-weight: 500; color: #0f1f2e; letter-spacing: -.01em; }
  .rmc-label { font-size: .6rem; letter-spacing: .12em; text-transform: uppercase; color: #a8885a; margin-top: 4px; font-weight: 500; }
  .rmc-sub   { font-size: .65rem; color: #7a6e65; margin-top: 3px; }
  .res-metric.rmc--green .rmc-val { color: #1D9E75; }
  .res-metric.rmc--amber .rmc-val { color: #b07a10; }
  .res-metric.rmc--red   .rmc-val { color: #E24B4A; }

  .res-chart-row { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .res-chart-card { background: rgba(15,31,46,.02); border: 1px solid rgba(15,31,46,.08); border-radius: 2px; padding: 14px; }
  .rcc-title { font-size: .62rem; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; font-weight: 500; margin-bottom: 10px; }
  .rcc-footnote { font-size: .67rem; color: #7a6e65; font-style: italic; margin-top: 8px; line-height: 1.5; }
  .rcc-market-note { font-size: .67rem; color: rgba(15,31,46,.45); background: rgba(201,169,110,.06); border: 1px solid rgba(201,169,110,.15); border-radius: 2px; padding: 7px 10px; margin-top: 6px; line-height: 1.5; }

  .gauge-wrap { display: flex; justify-content: center; }
  .gauge-legend { display: flex; align-items: center; gap: 4px; font-size: .68rem; color: #7a6e65; justify-content: center; margin-top: 6px; flex-wrap: wrap; }
  .gauge-dot { display: inline-block; width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }

  .res-collapsible { border: 1px solid rgba(15,31,46,.08); border-radius: 2px; }
  .res-collapsible + .res-collapsible { margin-top: 0; }
  .res-coll-hd { display: flex; align-items: center; justify-content: space-between; padding: 10px 14px; cursor: pointer; list-style: none; font-size: .65rem; font-weight: 500; letter-spacing: .14em; text-transform: uppercase; color: #a8885a; user-select: none; }
  .res-coll-hd::-webkit-details-marker { display: none; }
  .res-coll-hd svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; transition: transform .2s; flex-shrink: 0; }
  .res-collapsible[open] > .res-coll-hd svg { transform: rotate(180deg); }
  .res-coll-body { padding: 0 14px 14px; border-top: 1px solid rgba(15,31,46,.06); }

  @media (max-width: 1024px) { .calc-layout { grid-template-columns: 1fr; } }
  @media (max-width: 800px)  { .res-metrics { grid-template-columns: repeat(2, 1fr); } .res-chart-row { grid-template-columns: 1fr; } }
  @media (max-width: 600px)  { .portal-content { padding: 32px 5vw 60px; } .field-row { grid-template-columns: 1fr; } .calc-panel, .output-panel { padding: 18px 14px; } .res-metrics { grid-template-columns: repeat(2, 1fr); } }
</style>
